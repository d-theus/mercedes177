%h1 Новый заказ на сайте mercedes177.ru

%p
  Уважаемый #{@order.first_name} #{@order.middle_name}, ваш заказ
  принят в обработку.
%p
  Номер заказа: #{@order.id}
%p
  Позиции:
- @order.positions.each do |pos|
  %p #{pos.item.name}[#{pos.item.serial}] x#{pos.count}
