= simple_form_for @contact, url: '/contact', html: { class: 'form-horizontal'} do |f|
  = f.input :name
  = f.input :phone
  = f.input :email
  = f.input :text, as: :text
  = f.submit 'Отправить', class: 'btn btn-primary'
